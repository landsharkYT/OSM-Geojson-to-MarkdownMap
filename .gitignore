# .NET build output
bin/
obj/
*.user

# graphify (generated locally; refreshed by post-commit hook, not versioned)
graphify-out/

# sample / real-location data — never committed (privacy: avoids tying the repo to a
# real place). Bring your own .osm locally; tests skip when none is present.
*.osm
*.pbf
samples/
ProposedMap.md

# throwaway WASM feasibility spike (proved OsmSharp/NTS run in browser; not part of the build)
spike/

# web app artifacts
web/node_modules/
web/dist/
# generated by `npm run build:wasm`
web/public/dotnet/

# local tool state
.opencode/
.claude/settings.local.json
.opencode

# superseded design-corpus / scaffold docs — kept locally, untracked (repo cleanup pass)
OLDexamplemap.md
docs/impl-step-*.md
web/README.md
web/public/icons.svg

# playwright
web/test-results/
web/playwright-report/
web/blob-report/
.last-run.json
